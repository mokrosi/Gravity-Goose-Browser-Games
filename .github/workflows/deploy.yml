name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

# Grant GITHUB_TOKEN permission for Pages deployment
permissions:
  contents: read
  pages: write
  id-token: write

# Deploy only the latest commit; cancel any in-flight Pages runs
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  test:
    name: Run test suite
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Run tests
        run: |
          set -e
          for f in tests/*.test.js; do node "$f"; done

  deploy:
    name: Deploy to GitHub Pages
    needs: test
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Configure Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # The game is a static, dependency-free site at the repo root.
          path: .
          # Unique name per run avoids colliding with stale artifacts left by
          # cancelled runs (which otherwise breaks deploy-pages with
          # "Multiple artifacts named github-pages").
          name: github-pages-${{ github.run_id }}
          retention-days: 1
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
        with:
          artifact_name: github-pages-${{ github.run_id }}
